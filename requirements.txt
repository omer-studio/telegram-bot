# requirements.txt — כל הספריות שהבוט צריך בענן
# ---------------------------------------------------
# קובץ זה מגדיר את כל הספריות (חבילות) שיותקנו אוטומטית ב־Render.com
# אל תמחוק שורות בלי לוודא שהבוט לא צריך אותן!
# תיאור קצר של כל ספריה מופיע ליד — למחוק ספריה רק אם אתה בטוח.



python-telegram-bot==20.3     # ספריה להפעלת בוט טלגרם בפייתון (העיקרי שלך)
openai==1.3.5                 # ממשק API לצ'אט GPT של OpenAI
fastapi==0.95.1               # בניית Web API מהיר (משמש ל-webhook)
uvicorn==0.22.0               # שרת להרצת FastAPI בענן (מבצע listen)
gspread==5.12.0               # עבודה עם Google Sheets דרך פייתון
oauth2client==4.1.3           # התחברות מאובטחת ל-Google API (אימות OAuth)
python-dotenv==1.0.1          # טעינת משתני סביבה (ENV) מקובץ .env
telegram                      # עוד ספריה לטלגרם (לא חובה אם משתמשים רק ב-python-telegram-bot)
Flask                         # שרת Web פשוט (לא נחוץ אצלך כרגע, אפשר למחוק)
python-dateutil               # עוזר בעבודה עם תאריכים ושעות
APScheduler                   # מתזמן משימות (הרצת דוחות יומיים אוטומטית)
pytz                          # טיפול באזורי זמן (שעות ודקות לפי מדינה)
